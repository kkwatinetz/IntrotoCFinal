#include <stdio.h> //library

int main() //return int
{
    int x = 0,y = -1;
    int sum, product;

    printf("Enter first number ");
    scanf("%d", &x)
    
    printf("Enter second number ");
    scanf("%d", &y)

    sum = x + y;
    product x*y; 

    printf("The sum is: " + sum);
    printf("The sum is %d", sum);

    printf("The product is: " + product);

    printf("Goodbye");

    //  \ - tab \n - new line  

    return 0;
}
//float - %.2f (first two digits after decimal)
//float z = 3/2 -- %f\n, z - float/float = float
// ++z z++(after print +1) order of operations